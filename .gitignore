node_modules
dist
.turbo
.next
coverage

.env
.env.local
.env.*.local
.env.dokploy
!.env.example

*.log
.DS_Store
.vscode
.idea

# TypeScript incremental build cache — machine-local, never commit.
*.tsbuildinfo

# Backups only — DO NOT add a blanket *.sql here, it silently drops
# prisma migrations. Learned that the hard way.
backups/
*.dump
dump.rdb

# Editor/tool backup files. Six of these were committed by accident in
# #255 (a `cp "$f" "$f.bak"` before an in-place edit).
*.bak

# Vitest writes these next to a config when it transpiles it; they are per-run.
*.timestamp-*.mjs
